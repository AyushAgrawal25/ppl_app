import 'package:flutter/material.dart';

class OwnerCard extends StatelessWidget {
  const OwnerCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [
          // profile pic.

          // Info.
          Column(
            children: [
              // Name.

              // Age.
            ],
          )
        ],
      ),
    );
  }
}
